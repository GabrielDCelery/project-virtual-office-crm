version: '3'
services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: postgres
  backend:
    build:
      context: ../app/backend
      dockerfile: $PWD/backend.Dockerfile
      args:
        APP_CODE: /usr/src/virtual_office_crm_backend
    depends_on:
      - db
    ports:
      - 8080:8080
    environment:
      APP_PORT: 8080
      DB_CLIENT: pg
      DB_USER: postgres
      DB_HOST: db
      DB_PASSWORD: docker
      DB_NAME: postgres